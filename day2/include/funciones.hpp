#include <string>

int foo(int a);

bool partida_posible(std::string linea, int max_red, int max_green,
                     int max_blue);
int suma_posibles_partidas(std::string archivo);

int powerOfSetOfCubes(std::string linea);
